-r requirements.txt

# Testing + linting
pytest>=8.0
ruff>=0.6

# Serving (P4) — FastAPI app + test client
fastapi>=0.115
httpx>=0.27
uvicorn>=0.30

# LangChain integration (tools + retriever + LCEL RAG chain)
langchain-core>=0.3

# Edge (P4) ONNX export — optional; enable to run edge/export_onnx.py + its test:
#   scikit-learn>=1.5
#   skl2onnx>=1.17
#   onnx>=1.16
#   onnxruntime>=1.18

# Optional LLM backends (NOT needed for the default rule-based agent or CI).
# Install only if you want to drive the agent with a hosted or local LLM:
#   anthropic>=0.39      # ANTHROPIC_API_KEY
#   openai>=1.40         # OPENAI_API_KEY
# Optional local LLM for real RAG generation (scripts/rag_demo.py) and the
# MoLFormer embedding backend — CPU/MPS capable, downloads model weights:
#   transformers>=4.44
#   torch>=2.2
# Optional edge/on-device inference (P2/edge):
#   onnx>=1.16
#   onnxruntime>=1.18
